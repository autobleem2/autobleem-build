# a fine-grained personal access token for autobleem/AutoBleem2 with "Administration: read and write"
# (the runner registers itself with it); never commit the filled-in .env
ACCESS_TOKEN=
# where the runner keeps its checkouts and build dirs - the same path inside and outside the container
RUNNER_WORK=/home/claude/actions-runner/work
